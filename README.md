# This is a repo dedicated to learn and test sql 
## Create an artificate of most recent PostgreSQL db 
pg_dump -U <your-name> -d testdb > testdb.sql
doing this replace the old db with new one. Since git tracks changes this is safe for testing.

